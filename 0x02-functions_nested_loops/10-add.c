#include <stdio.h>
#include "main.h"
/**
 * add - prints the sum of two numbers
 *@x: integer parameter
 *@y: integer parameter
 * Return: returns sum of two numbers
*/
int add(int x, int y)
{
	return (x + y);
}
