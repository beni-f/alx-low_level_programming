#include <stdio.h>
#include "main.h"
/**
 * print_sign - checks if a number is positive, negative or zero
 *@n: integer parameter 
 * Return: returns 0
*/
int print_sign(int n)
{
	return (n > 0 || n < 0);
}
