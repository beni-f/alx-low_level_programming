#include <stdio.h>
#include "main.h"
/**
 * _islower - checks whether the character is lower or uppercase
 *@c: integer parameter
 * Return: returns int
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
